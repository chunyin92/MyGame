using UnityEngine;

namespace RPG.CharacterSystem.Editor
{
    public partial class CharacterEditor
    {
        void TopTabBar()
        {
            //GUILayout.BeginHorizontal("Box", GUILayout.ExpandWidth(true));
            //GUILayout.Space(1070);
            //if (GUILayout.Button ("Refreah", GUILayout.Width (200)))
            //{
            //    for (int i = 0; i < _characterDatabase.Count; i++)
            //    {
            //        BaseCharacter temp = new BaseCharacter ();

            //        temp.Clone (_characterDatabase.Get (i));
            //        //temp.OnGUI ();

            //        for (int j = 0; j < temp.BaseSkill.Count; j++)
            //        {
            //            temp.BaseSkill[j] = skilldb.Get (skilldb.GetIndex (temp.BaseSkill[j].Name));
            //        }

            //        _characterDatabase.Replace (i, temp);
            //    }
            //    Debug.Log ("Refresh " + _characterDatabase.Count + " Character Data");
            //}
            //GUILayout.EndHorizontal();
        }
    }	
}
